namespace TouchIT.Entity
{
    public enum GameState
    {
        MainMenu,       // 구체 둥둥 떠있는 상태
        StageSelect,    // 구체 안으로 들어온 상태 (앨범 고르기)
        InGame,         // 리듬 게임 진행 중
        Result          // 결과 화면
    }
}